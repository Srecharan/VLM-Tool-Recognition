apiVersion: v1
kind: Service
metadata:
  name: vlm-tool-service
  labels:
    app: vlm-tool-recognition
spec:
  type: NodePort
  ports:
  - port: 8000
    targetPort: 8000
    nodePort: 30080
  selector:
    app: vlm-tool-recognition 